# Release 1.2.0 - Complete Markdown Edition

This edition is designed for direct GitHub upload.

## Included

- complete 6,000+ word visual engineering white paper at the repository root;
- complete 12,000+ word technical paper with theorem statements and proofs in Markdown;
- no PDF, TeX, bibliography source or paper-build artifact;
- no `.gitignore` file and no `.github` directory;
- complete visible experiment runners rather than placeholder launchers;
- installable serial and memory-mapped MapReduce implementation;
- one-million-agent raw and aggregate reference results;
- exactness benchmarks and brute-force validation data;
- quick and full one-command reproduction profiles;
- Docker, Conda, pinned dependencies, tests and integrity hashes;
- GitHub figures and LinkedIn release assets.

## Reproducibility contract

The terminal answer is always an index into an actual produced output. Correctness is evaluator-only. Accuracy is checked statistically against frozen references, while timing is rerun and validated on the host machine.
