# Contributing

Open an issue before substantial changes. Every pull request should include:

1. a precise description of the mathematical or implementation change;
2. tests covering altered behaviour;
3. regenerated tables or figures when results change;
4. confirmation that the terminal result remains an actual produced agent output.

Run `make test` and `make quick` before submitting.
